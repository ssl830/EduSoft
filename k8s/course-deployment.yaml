apiVersion: apps/v1
kind: Deployment
metadata:
  name: course-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: course-service
  template:
    metadata:
      labels:
        app: course-service
    spec:
      containers:
        - name: course-service
          image: fankeqing/edusoft:edusoft-course-service
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://edusoft-mysql:3306/course_db?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai"
            - name: SPRING_DATASOURCE_USERNAME
              value: "root"
            - name: SPRING_DATASOURCE_PASSWORD
              value: "fkq200566"
            - name: SERVICES_USER_BASE_URL
              value: "http://user-service:8081"
            - name: SERVICES_CONTENT_URL
              value: "http://content-service:8083"
            - name: SERVICES_LEARNING_URL
              value: "http://learning-service:8084"
---
apiVersion: v1
kind: Service
metadata:
  name: course-service
spec:
  type: NodePort
  selector:
    app: course-service
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
      nodePort: 30082