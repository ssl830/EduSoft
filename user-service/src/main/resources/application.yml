server:
  port: 8081

spring:
  application:
    name: user-service
  
  datasource:
    url: jdbc:mysql://localhost:3306/courseplatform?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: lyt999999
    driver-class-name: com.mysql.cj.jdbc.Driver

# CORS配置
  web:
    cors:
      allowed-origins: "*"
      allowed-methods: "*"
      allowed-headers: "*"
      allow-credentials: true
      max-age: 3600

# 日志配置
logging:
  level:
    org.example.edusoft.user: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: "*"
